/*代码转换核心部分*/function getLink(){        var skydrive_old = document.getElementById("skydrive_old");        if(skydrive_old.value.length == 0){        alert("链接不能为空");        skydrive_old.focus();        return false;    }        skydrive_old = skydrive_old.value.split("=");       var skydrive_song = document.getElementById("skydrive_song");       if(skydrive_song.value.length == 0){        alert("文件名不能为空");        skydrive_song.focus();        return false;    }     skydrive_song = encodeURI(skydrive_song.value);       document.getElementById("skydrive_new").value = "http://storage.live.com/items/"+(skydrive_old[1])+"?filename="+skydrive_song+"";}